import React from "react";
import Topnav from "../Layout/Topnav";

const About = () => {
    return (
        <section className={`main`}>
            <Topnav />
            about
        </section>
    );
};

export default About;
